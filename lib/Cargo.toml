[package]
name = "zeth-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
ethers-core = { version = "2.0", features = ["optimism"] }
hashbrown = { workspace = true }
once_cell = "1.18"
revm = { workspace = true }
ruint = { version = "1.10", default-features = false }
serde = "1.0"
thiserror = "1.0"
zeth-primitives = { path = "../primitives", features = ["revm"] }

[target.'cfg(not(target_os = "zkvm"))'.dependencies]
chrono = { version = "0.4", default-features = false, optional = true }
ethers-providers = { version = "2.0", features = ["optimism"], optional = true }
flate2 = { version = "1.0.26", optional = true }
log = { version = "0.4", optional = true }
serde_json = { version = "1.0", default-features = false, features = ["alloc"], optional = true }
serde_with = { version = "3.0", optional = true }
tokio = { version = "1.23", features = ["full"], optional = true }
zeth-primitives = { path = "../primitives", features = ["revm", "ethers"], optional = true }

[dev-dependencies]
bincode = "1.3"
serde_with = "3.1"

[features]
default = ["host"]
host = [
    "dep:chrono",
    "dep:ethers-providers",
    "dep:flate2",
    "dep:log",
    "dep:serde_json",
    "dep:serde_with",
    "dep:tokio",
    "dep:zeth-primitives"
]