[package]
name = "zeth-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
alloy-sol-types = { version = "0.6", default-features = false}
anyhow = { version = "1.0", default-features = false }
bytes = { version = "1.5", default-features = false }
ethers-core = { version = "2.0", features = ["optimism"] }
hashbrown = { workspace = true }
libflate = {version = "2.0.0", default-features = false }
core2 = { version = "0.4", default_features = false, features = ["alloc"]}
once_cell = { version = "1.18", default-features = false }
revm = { workspace = true }
ruint = { version = "1.10", default-features = false }
serde = { version = "1.0", default-features = false, features = ["alloc"] }
thiserror-no-std = "2.0.2"
zeth-primitives = { path = "../primitives", default_features = false,  features = ["revm", "ethers"] }

[target.'cfg(not(target_os = "zkvm"))'.dependencies]
chrono = { version = "0.4", default-features = false }
log = { version = "0.4", default-features = false }
tokio = { version = "1.23", features = ["full"] }
zeth-primitives = { path = "../primitives", features = ["revm", "ethers"] }

[dev-dependencies]
bincode = "1.3"

[features]
# default = ["std"]
std = []