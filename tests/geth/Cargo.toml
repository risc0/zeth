[package]
name = "tests"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
zeth-primitives = { version = "0.1.0", path = "../../primitives" }
serde_json = "1.0"
serde = "*"
hex = "*"

[build-dependencies]
gobuild = "0.1.0-alpha.1"
log = "0.4.14"
env_logger = "0.10.2"
